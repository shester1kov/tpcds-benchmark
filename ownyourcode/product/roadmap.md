# Project Roadmap

## Current Status

Существующий проект. Добавляем новый модуль — загрузку результатов бенчмарка в Ozone S3.

---

## Фаза 1: Конфигурация ✅ ЗАВЕРШЕНА (2026-02-17)

**Приоритет:** ВЫСОКИЙ

- [x] Добавить структуру `S3Config` в `pkg/config/config.go`
- [x] Добавить `S3Config` в основную структуру `Config`
- [x] Добавить секцию `s3_config:` в `config/config.yaml` с примером значений
- [x] Добавить `S3Config` в `config/config.yaml.example`

---

## Фаза 2: S3 Storage модуль

**Приоритет:** ВЫСОКИЙ

- [ ] Установить зависимость `github.com/minio/minio-go/v7`
- [ ] Добавить метод `GetFilePath()` в `pkg/storage/csv_storage.go`
- [ ] Создать `pkg/storage/s3_storage.go` с клиентом minio
- [ ] Реализовать функцию загрузки файла в бакет

---

## Фаза 3: Интеграция в бенчмарк

**Приоритет:** СРЕДНИЙ

- [ ] В `pkg/runner/benchmark.go` после завершения — вызвать загрузку в S3
- [ ] Проверить флаг `enabled` перед загрузкой
- [ ] Добавить логирование: успех или ошибка с понятным сообщением
- [ ] Протестировать на реальном Ozone S3 endpoint
