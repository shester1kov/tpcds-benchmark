# TPC-DS Benchmark - C4 Architecture Documentation

Эта директория содержит архитектурные диаграммы проекта TPC-DS Benchmark в нотации C4, описанные в двух форматах:
- **Structurizr DSL** (`workspace.dsl`) - полная модель со всеми уровнями абстракции
- **PlantUML** (`*.puml` файлы) - отдельные диаграммы для быстрого просмотра

## 📁 Структура файлов

```
c4-architecture/
├── workspace.dsl                    # Полная C4 модель (Structurizr DSL)
├── c4-system-context.puml           # System Context диаграмма (PlantUML)
├── c4-containers.puml               # Container диаграмма (PlantUML)
├── c4-components.puml               # Component диаграмма (PlantUML)
├── sequence-benchmark-flow.puml     # Sequence диаграмма потока выполнения
├── view-diagrams.sh                 # Helper скрипт для просмотра диаграмм
└── README.md                        # Эта документация
```

## 📋 Описание диаграмм

### Structurizr DSL (`workspace.dsl`)

Файл `workspace.dsl` содержит следующие представления (views):

### 1. **System Context** (Системный контекст)
Показывает общий обзор системы и ее взаимодействие с пользователями и внешними хранилищами данных (Trino, Hive, Impala, Vertica).

### 2. **Containers** (Контейнеры)
Детализирует основные контейнеры системы:
- CLI Application (Go приложение)
- Configuration File (YAML конфигурация)
- Queries Storage (TPC-DS SQL запросы)
- Results Storage (CSV результаты)

### 3. **Components** (Компоненты CLI приложения)
Показывает детальную архитектуру Go приложения с основными компонентами:
- Main Controller
- Config Loader
- Connection Manager
- Executor Factory
- SQL/Hive Executors
- Query Loader
- Benchmark Runner
- CSV Storage

### 4. **Connection Manager Components**
Детальная диаграмма Connection Manager с коннекторами для каждой БД.

### 5. **Benchmark Execution Flow** (Динамическая диаграмма)
Показывает последовательность выполнения бенчмарка от запуска до сохранения результатов.

### 6. **Production Deployment**
Диаграмма развертывания в production окружении.

### PlantUML файлы (`.puml`)

PlantUML файлы можно использовать напрямую в различных инструментах:

- **c4-system-context.puml** - системный контекст с внешними системами
- **c4-containers.puml** - основные контейнеры приложения
- **c4-components.puml** - компоненты CLI приложения
- **sequence-benchmark-flow.puml** - последовательность выполнения бенчмарка

## 🚀 Как визуализировать диаграммы

### 🎯 Быстрый старт (рекомендуется)

Используйте helper скрипт для интерактивного выбора метода визуализации:

```bash
cd c4-architecture
./view-diagrams.sh
```

Скрипт предложит выбрать один из вариантов:
1. Запустить Structurizr Lite через Docker
2. Сгенерировать PNG из PlantUML
3. Сгенерировать SVG из PlantUML
4. Открыть PlantUML онлайн редактор

### PlantUML файлы

#### Онлайн визуализация
- [PlantUML Web Server](https://www.plantuml.com/plantuml/uml/) - вставьте код из `.puml` файла
- [PlantText](https://www.planttext.com/) - альтернативный онлайн редактор

#### VS Code
1. Установите плагин [PlantUML](https://marketplace.visualstudio.com/items?itemName=jebbs.plantuml)
2. Откройте файл `.puml`
3. Нажмите `Alt+D` для preview

#### IntelliJ IDEA / PyCharm
1. Установите плагин [PlantUML integration](https://plugins.jetbrains.com/plugin/7017-plantuml-integration)
2. Откройте файл `.puml` - preview появится автоматически

#### Командная строка
```bash
# Установка PlantUML
brew install plantuml  # macOS
apt-get install plantuml  # Ubuntu/Debian

# Генерация PNG
plantuml c4-system-context.puml

# Генерация SVG
plantuml -tsvg c4-system-context.puml
```

#### GitHub/GitLab
PlantUML диаграммы автоматически рендерятся в README файлах:
```markdown
![System Context](c4-architecture/c4-system-context.puml)
```

### Structurizr DSL файлы

### Вариант 1: Structurizr Lite (Рекомендуется для локальной работы)

Structurizr Lite - это бесплатный локальный веб-сервер для визуализации C4 диаграмм.

```bash
# Запуск через Docker
docker run -it --rm -p 8080:8080 \
  -v $(pwd)/c4-architecture:/usr/local/structurizr \
  structurizr/lite

# Откройте в браузере
open http://localhost:8080
```

### Вариант 2: VS Code плагин

1. Установите плагин [Structurizr DSL Extension](https://marketplace.visualstudio.com/items?itemName=systemticks.c4-dsl-extension)
2. Откройте файл `workspace.dsl`
3. Нажмите на иконку preview или используйте команду `Structurizr: Preview`

### Вариант 3: Structurizr Cloud (Онлайн сервис)

1. Зарегистрируйтесь на [structurizr.com](https://structurizr.com/)
2. Создайте новый workspace
3. Скопируйте содержимое `workspace.dsl` в онлайн редактор

### Вариант 4: Конвертация в PlantUML

Используйте [structurizr-export](https://github.com/structurizr/export) для конвертации в PlantUML:

```bash
# Установка
npm install -g @structurizr/cli

# Экспорт в PlantUML
structurizr-cli export -workspace workspace.dsl -format plantuml
```

## 📚 Дополнительные ресурсы

- [C4 Model](https://c4model.com/) - официальная документация по нотации C4
- [Structurizr DSL](https://github.com/structurizr/dsl) - документация по языку DSL
- [Structurizr Lite](https://structurizr.com/help/lite) - руководство по Structurizr Lite

## 🏗️ Архитектурные принципы проекта

### Основные компоненты:

1. **Connection Manager** - управление подключениями с retry логикой и TLS
2. **Executor Factory** - паттерн Factory для создания специализированных исполнителей
3. **Query Loader** - загрузка TPC-DS запросов из файловой системы
4. **Benchmark Runner** - многопоточная оркестрация выполнения бенчмарков
5. **CSV Storage** - потокобезопасное сохранение результатов

### Поддерживаемые хранилища данных:

- **Trino** - распределенный SQL движок
- **Hive** - Apache Hive с HiveServer2
- **Spark** - Apache Spark через Hive Thrift
- **Impala** - Cloudera Impala MPP
- **Vertica** - аналитическая СУБД Vertica

### Особенности архитектуры:

- ✅ Многопоточность (configurable concurrency)
- ✅ Retry механизм для подключений
- ✅ TLS поддержка
- ✅ Гибкая конфигурация через YAML
- ✅ Раздельные executor'ы для разных типов БД
- ✅ Потокобезопасное сохранение результатов
- ✅ Graceful shutdown

## 🔄 Обновление диаграмм

При изменении архитектуры проекта, обновите файл `workspace.dsl`:

1. Отредактируйте `workspace.dsl`
2. Проверьте изменения через Structurizr Lite
3. Зафиксируйте изменения в Git

## 📝 Примеры использования

### Просмотр только определенной диаграммы

В Structurizr Lite используйте навигацию слева для переключения между views:
- System Context
- Containers
- Components
- Connection Manager Components
- Benchmark Execution Flow
- Production Deployment

### Экспорт диаграмм в изображения

```bash
# Через Structurizr CLI
structurizr-cli export \
  -workspace workspace.dsl \
  -format png \
  -output ./diagrams
```

## 🤝 Вклад в документацию

При добавлении новых компонентов или изменении архитектуры:

1. Обновите `workspace.dsl`
2. Добавьте описание изменений в этот README
3. Создайте Pull Request с обновленными диаграммами
